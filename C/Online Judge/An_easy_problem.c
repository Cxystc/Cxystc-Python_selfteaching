#include <stdio.h>

int main (){
    int k;
    long long n ; 
    scanf("%d %lld",&k,&n);
    long long m ; 
    scanf("%lld",&m);
    for ( int i = 0 ; i < m ;i ++){
        long long pos ; 
        int num; 
        scanf("%lld %d",&pos,&num);
    }
}