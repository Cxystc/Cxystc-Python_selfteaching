#include <math.h>
#include <stdio.h>

int main()
{
    long long int n;
    long long int k;
    long long int j;
    int t;
    scanf("%d", &t);
    for (int i = 0; i < t; i++)
    {
        scanf("%lld %lld", &n, &k);
    }
    j = pow(9,k);
    j = pow(n,j);
    printf("%lld",);
}